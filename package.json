{
  "name": "site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p build && cp -R public/* build || true",
    "start": "npx serve build"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
